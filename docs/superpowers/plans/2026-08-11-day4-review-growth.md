# CatstepCoach Day 4 实施计划

## 全局约束

- 直接在当前工作目录开发；不创建 worktree，不提交、不推送、不创建 PR，也不删除分支。
- 每个行为采用单个红—绿—重构循环：先运行新增测试并确认失败，再写最小实现并复跑。
- 不硬编码或提交 AppID、环境 ID、TokenHub 凭据、`project.private.config.json` 或真实用户数据。

## 任务

1. **复盘生成**：为 `review-generate` 编写服务、处理器和结构测试；实现仅查询本人当天计划、结构化 AI 复盘与规则降级；验证聚焦测试、类型检查与云函数构建。
2. **复盘确认与记忆**：为 `review-confirm` 编写幂等保存、可选记忆确认和用户隔离测试；实现原子写入；验证重复请求不重复写入。
3. **成长值**：先测试首次确认复盘的确定性加分、上限与不扣分规则；实现服务端累计；验证重放不重复加分。
4. **逾期调整**：先测试本人未完成任务的缩小/重排、归属校验与模型失败降级；实现独立 `resizeTask` 工作流和持久化更新。
5. **客户端与收尾**：测试复盘入口、加载/错误/确认状态及成长结果；完成最小 Today UI，运行全量测试、类型检查、云函数构建、差异检查和私密文件检查。

## 当前执行位置

任务 1 至任务 5 已完成；Day 4 已通过自动化测试、类型检查和云函数构建收尾验证。
